import { SensorTypeToTextPipe } from './sensor-type-to-text.pipe';

describe('SensorTypeToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SensorTypeToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
