import { InvalidUntouchedMatcher } from './invalid-untouched-matcher';

describe('InvalidUntouchedMatcher', () => {
  it('should create an instance', () => {
    expect(new InvalidUntouchedMatcher()).toBeTruthy();
  });
});
